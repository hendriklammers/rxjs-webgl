{
  "name": "rxjs-webgl",
  "version": "0.0.1",
  "description": "Playing with webgl and rxjs",
  "main": "src/main.js",
  "scripts": {
    "build": "npm install && npm run lint && webpack --optimize-minimize",
    "lint": "eslint src --ext .js",
    "start": "npm run watch",
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "webpack-dev-server --port 3000 --inline --content-base public/"
  },
  "babel": {
    "presets": [
      "es2015-webpack"
    ]
  },
  "author": "Hendrik Lammers",
  "license": "MIT",
  "devDependencies": {
    "babel-core": "^6.9.1",
    "babel-loader": "^6.2.4",
    "babel-polyfill": "^6.9.1",
    "babel-preset-es2015": "^6.9.0",
    "babel-preset-es2015-webpack": "^6.4.1",
    "eslint": "^2.11.1",
    "webpack": "^2.1.0-beta.8",
    "webpack-dev-server": "^2.1.0-beta"
  },
  "dependencies": {
    "lodash": "^4.13.1",
    "rxjs": "^5.0.0-beta.8"
  }
}
